# Flight-Viz 環境変数テンプレート

# ===================================
# ローカル開発環境用（Docker Compose）
# ===================================
# このファイルをコピーして .env を作成してください

# Rails 環境
RAILS_ENV=development

# PostgreSQL データベース設定
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=flight_viz_development

# ドメイン設定（開発環境）
FRONTEND_URL=http://localhost:3001
BACKEND_URL=http://localhost:3000

# ===================================
# 本番環境用（Kubernetes）
# ===================================
# 本番環境の設定は k8s/backend/secret.yaml で管理されます
#
# 変更が必要な場合：
# 1. k8s/backend/secret.yaml を編集（base64エンコードが必要）
# 2. kubectl apply -f k8s/backend/secret.yaml
# 3. kubectl rollout restart deployment/backend -n flight-viz
#
# 主な設定項目：
# - POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB
# - SECRET_KEY_BASE
# - DockerHub認証情報（.dockerconfigjson）
